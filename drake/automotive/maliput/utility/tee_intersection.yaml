# -*- yaml -*-
---
# distances are meters; angles are degrees.
maliput_monolane_builder:
  id: tee_intersection
  lane_bounds: [-2, 2]
  driveable_bounds: [-4, 4]
  points:
    start:
      xypoint: [0, 45, 0]  # x,y, heading
      zpoint: [0, 0, 0.4, 0]  # z, zdot, theta (superelevation), thetadot
  connections:
    # TODO(rico) I don't quite know how this should look yet.
        
  
